---
const allPosts = await Astro.glob("../pages/pigeons/species/*.md");
---

<ul>
  {
    allPosts.map((post) => (
      <li>
        <a href={`${post.url}/`}>{post.frontmatter.title}</a>
      </li>
    ))
  }
</ul>

<style>
  li {
    margin: 16px;
  }

  a {
    font-size: 24px;
  }
</style>
